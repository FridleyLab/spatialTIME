% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bi_nn_g.R
\name{bi_NN_G}
\alias{bi_NN_G}
\title{Bivariate Nearest Neighbor G(r)}
\usage{
bi_NN_G(
  mif,
  mnames,
  r_range = 0:100,
  num_permutations = 50,
  edge_correction = "rs",
  keep_permutation_distribution = FALSE,
  workers = 1,
  overwrite = FALSE,
  xloc = NULL,
  yloc = NULL,
  ...
)
}
\arguments{
\item{mif}{object of class \code{mif} created by function \code{create_mif()}}

\item{mnames}{character vector of column names within the spatial files
indicating whether a cell is positive for a phenotype, or a two-column data
frame of specific anchor/counted combinations}

\item{r_range}{numeric vector of radii at which to evaluate G(r)}

\item{num_permutations}{integer number of permutations used to estimate
sample-specific complete spatial randomness (CSR)}

\item{edge_correction}{edge correction method: one of "rs", "km", "han" or "none"}

\item{keep_permutation_distribution}{boolean; keep each permutation's result or
average them to one row per marker pair and radius}

\item{workers}{integer number of CPU cores used to process samples in parallel}

\item{overwrite}{boolean; replace an existing \code{bivariate_NN} slot rather than
appending it as a new \code{Run}}

\item{xloc, yloc}{the x and y columns giving cell centres. If left \code{NULL},
\code{XMin}, \code{XMax}, \code{YMin} and \code{YMax} must be present.}

\item{...}{support for deprecated argument names. \code{keep_perm_dis} is accepted as
an alias for \code{keep_permutation_distribution}. Anything else is an error.}
}
\value{
object of class \code{mif} with a \code{bivariate_NN} table in the \code{derived} slot
}
\description{
\code{bi_NN_G()} computes the cross-type nearest-neighbour distribution function:
for each ordered pair of markers, the proportion of anchor-positive cells whose
nearest \emph{counted}-positive neighbour lies within r. Cells positive for both
markers of a pair are excluded from that pair, so the anchor and counted sets
are always disjoint.
}
\section{Accuracy}{

Estimates come from \code{\link[spatstat.explore:Gcross]{spatstat.explore::Gcross()}}. Before 2.0.0 this function
hand-rolled the \code{rs} and \code{han} estimators on a full \code{as.matrix(dist(...))},
needing memory proportional to the square of the cell count. Those hand-rolled
results agreed with \code{Gcross()} exactly on well-populated markers, but returned
\code{NaN} where \code{Gcross()} correctly returns \code{0} for sparse markers, and the \code{rs}
branch referenced an undefined variable. Delegating fixes the sparse-marker
case and drops memory to O(n).

The observation window is the convex hull of \strong{every} cell in the sample and is
held fixed across all marker pairs and permutations.
}

\section{Why there is no exact CSR for G}{

See the corresponding section of \code{\link[=NN_G]{NN_G()}}. The \verb{Exact CSR} column exists for
schema consistency with \code{\link[=ripleys_k]{ripleys_k()}} and is always \code{NA}.
}

\examples{
x <- spatialTIME::create_mif(clinical_data = spatialTIME::example_clinical \%>\%
  dplyr::mutate(deidentified_id = as.character(deidentified_id)),
  sample_data = spatialTIME::example_summary \%>\%
  dplyr::mutate(deidentified_id = as.character(deidentified_id)),
  spatial_list = spatialTIME::example_spatial[1],
  patient_id = "deidentified_id",
  sample_id = "deidentified_sample")

x2 = bi_NN_G(mif = x,
      mnames = c("CD3..Opal.570..Positive", "CD8..Opal.520..Positive"),
      r_range = seq(0, 100, 10), num_permutations = 10,
      edge_correction = "rs", workers = 1, overwrite = TRUE)
}
