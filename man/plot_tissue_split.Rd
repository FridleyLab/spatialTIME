% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_tissue_split.R
\name{plot_tissue_split}
\alias{plot_tissue_split}
\title{Plot density-based tissue compartments alongside the density difference}
\usage{
plot_tissue_split(
  mif,
  which = NULL,
  compartment = c("refined_density_compartment", "density_compartment"),
  panels = c("both", "density", "compartment"),
  colors = NULL,
  point_size = 0.4,
  raster_max_pixels = 5e+05,
  workers = 1,
  filename = NULL,
  path = NULL,
  settings = NULL,
  ...
)
}
\arguments{
\item{mif}{object of class \code{mif} that has been through \code{\link[=split_tissue]{split_tissue()}}}

\item{which}{samples to plot: \code{NULL} for all, or a numeric/character vector
indexing/naming elements of \code{mif$spatial}}

\item{compartment}{which compartment column to colour cells by: the
3-level \code{"refined_density_compartment"} (default) or the 2-level
\code{"density_compartment"}}

\item{panels}{which panel(s) to draw: \code{"both"} (default, one faceted plot
with the density-difference raster and the compartment scatter
side by side), \code{"density"} alone, or \code{"compartment"} alone}

\item{colors}{named character vector of colours for \code{class1}, \code{class2} and
(for \code{compartment = "refined_density_compartment"}) \code{"Interface"}. If
\code{NULL}, defaults to \code{RColorBrewer::brewer.pal(3, "Set1")[1:2]} for the two
classes and \code{"grey20"} for \code{"Interface"}; \code{NA}/unclassified cells are
always drawn \code{"grey70"}.}

\item{point_size}{size passed to \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}} for the compartment panel}

\item{raster_max_pixels}{the density raster is recomputed at plot time (see
Details) at up to this many pixels; above the cap resolution is capped
for display only -- the drawn boundary line is unaffected and stays exact.}

\item{workers}{number of cores to use, one sample per core}

\item{filename, path}{if \code{filename} is given, every requested plot is also
written to a single PDF at \code{file.path(path, filename)} (\code{path} defaults
to the working directory; a trailing \code{.pdf} in \code{filename} is not doubled).}

\item{settings}{provenance normally read from
\code{attr(mif$derived$density_boundary, "call_info")}, which \code{\link[=split_tissue]{split_tissue()}}
attaches. That attribute does not survive \code{[} subsetting or
\code{dplyr::bind_rows()} (e.g. after \code{\link[=merge_mifs]{merge_mifs()}} disagrees across mifs), so
pass the settings list explicitly to recover from that: a list with
\code{classifier}, \code{class1}, \code{class2}, \code{sigma}, \code{eps}, \code{interface_width},
\code{xloc}, \code{yloc}, \code{filter_density} and \code{sample_id}.}

\item{...}{accepts no arguments; present only so that a mistyped named
argument above produces an informative "Unknown argument" error instead
of being silently absorbed.}
}
\value{
a named list of \code{ggplot} objects, one per requested sample, named
like \code{mif$spatial} -- \strong{not} the \code{mif}. Unlike \code{\link[=plot_immunoflo]{plot_immunoflo()}}, this
deliberately does not attach the plots to \code{mif$derived}: each plot
captures a raster data frame that can be as large as the sample itself,
and \code{merge_mifs()}'s handling of list-valued derived slots is fragile
enough already (see \code{NEWS.md}) without doubling the exposure.
}
\description{
\code{\link[=split_tissue]{split_tissue()}} discards the density images and point patterns it computes
the boundary from, to avoid inflating the mif. \code{plot_tissue_split()}
recomputes them on demand, at plot time, from the settings recorded by
\code{\link[=split_tissue]{split_tissue()}}, and draws the \emph{stored} boundary polyline (never a
recontoured one) on top of both a raster of the density difference and a
scatter of the per-cell compartment label.
}
\examples{
library(dplyr)
x <- create_mif(clinical_data = example_clinical \%>\%
  mutate(deidentified_id = as.character(deidentified_id)),
  sample_data = example_summary \%>\%
  mutate(deidentified_id = as.character(deidentified_id)),
  spatial_list = example_spatial[1],
  patient_id = "deidentified_id",
  sample_id = "deidentified_sample")
x <- split_tissue(x, classifier = "Classifier.Label",
  class1 = "Tumor", class2 = "Stroma",
  sigma = 40, interface_width = 100)
plots <- plot_tissue_split(x)
}
